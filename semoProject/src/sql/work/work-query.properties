#work-properties
getListCount=SELECT COUNT(*) FROM TB_WORK WHERE SECRET_FLAG = 'N'
selectList=SELECT WORK_NO, WORK_TITLE, WRITER_NO, SERIAL_CNT, APPROVAL_DATE FROM TB_WORK WHERE SECRET_FLAG = 'N'
mondayList=SELECT WORK_NO, UPDATE_DAY, WORK_TITLE, WRITER_NO, SERIAL_CNT, APPROVAL_DATE FROM TB_WORK WHERE SECRET_FLAG ='N'AND UPDATE_DAY like ('%'||?||'%')
selectGenreList=SELECT WORK_NO, WORK_TITLE, WRITER_NO, SERIAL_CNT, APPROVAL_DATE FROM TB_WORK JOIN TB_GENRE_WORK USING(WORK_NO) WHERE SECRET_FLAG = 'N'
genreList=SELECT WORK_NO, WORK_TITLE, WRITER_NO, SERIAL_CNT, APPROVAL_DATE  FROM TB_WORK  JOIN TB_GENRE_WORK USING(WORK_NO)  JOIN TB_GENRE USING(GENRE_NO) WHERE SECRET_FLAG ='N'AND GENRE_NO like ('%'||?||'%')
selectSerial=SELECT WORK_NO, APPROVAL_DATE, WORK_SUMMARY,  WRITER_NO, WORK_TITLE, GENRE_NAME   FROM TB_WORK  JOIN TB_GENRE_WORK USING(WORK_NO)  JOIN TB_GENRE USING(GENRE_NO) WHERE WORK_NO =  ?
episodeList=SELECT EPISODE_NO, E.APPROVAL_DATE, VIEWS_CNT, WORK_TITLE, EPISODE_TITLE FROM TB_WORK W JOIN TB_EPISODE E USING(WORK_NO) WHERE WORK_NO = ?
episodeDetail=SELECT EPISODE_NO, WORK_TITLE, EPISODE_TITLE FROM TB_WORK JOIN TB_EPISODE USING(WORK_NO) WHERE EPISODE_NO = ?

#LSH
#admin
getWorkListCount=SELECT COUNT(*) FROM TB_WORK WHERE APPROVAL_STATUS = 'Y' AND SECRET_FLAG = 'N'
adminWorkList=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT WORK_TITLE, M.MEMBER_NICKNAME, W.WRITER_NO FROM TB_WORK W JOIN TB_WRITER WR ON (W.WRITER_NO = WR.MEMBER_NO) JOIN TB_MEMBER M ON (WR.MEMBER_NO = M.MEMBER_NO) WHERE APPROVAL_STATUS = 'Y' AND SECRET_FLAG = 'N' ORDER BY WORK_NO DESC) A) WHERE RNUM BETWEEN ? AND ?
getWorkSearchListCount=SELECT COUNT(*) FROM TB_WORK WHERE APPROVAL_STATUS = 'Y' AND SECRET_FLAG = 'N' AND WORK_TITLE LIKE ('%' || ? || '%')
adminWorkSearchList=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT WORK_TITLE, M.MEMBER_NICKNAME, W.WRITER_NO FROM TB_WORK W JOIN TB_WRITER WR ON (W.WRITER_NO = WR.MEMBER_NO) JOIN TB_MEMBER M ON (WR.MEMBER_NO = M.MEMBER_NO) WHERE APPROVAL_STATUS = 'Y' AND SECRET_FLAG = 'N' AND WORK_TITLE LIKE ('%' || ? || '%') ORDER BY WORK_NO DESC) A) WHERE RNUM BETWEEN ? AND ?
getAdminWorkWriterCount=SELECT COUNT(*) FROM TB_WORK W JOIN TB_WRITER WR ON (W.WRITER_NO = WR.MEMBER_NO) JOIN TB_MEMBER M ON (WR.MEMBER_NO = M.MEMBER_NO) WHERE W.WRITER_NO = ? AND APPROVAL_STATUS = 'Y'
selectAdminWorkWriterList=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT W.WRITER_NO, M.MEMBER_NICKNAME, W.WORK_TITLE, W.WORK_SUMMARY, W.WORK_NO, W.SECRET_FLAG FROM TB_WORK W JOIN TB_WRITER WR ON (W.WRITER_NO = WR.MEMBER_NO) JOIN TB_MEMBER M ON (WR.MEMBER_NO = M.MEMBER_NO) WHERE W.WRITER_NO = ? AND APPROVAL_STATUS = 'Y' ORDER BY W.WORK_NO DESC) A) WHERE RNUM BETWEEN ? AND ?
selectGenre=SELECT GENRE_NAME FROM TB_GENRE G JOIN TB_GENRE_WORK GW USING(GENRE_NO) WHERE WORK_NO = ?
#secretWorkConfirm=SELECT COUNT(*) FROM TB_WORK WHERE SECRET_FLAG = 'Y' AND WORK_NO IN (?)
getAdminWriterSearchCount=SELECT COUNT(*) FROM TB_WORK W JOIN TB_WRITER WR ON (W.WRITER_NO = WR.MEMBER_NO) JOIN TB_MEMBER M ON (WR.MEMBER_NO = M.MEMBER_NO) WHERE W.WORK_TITLE LIKE ('%' || ? || '%') AND W.WRITER_NO = ? AND APPROVAL_STATUS = 'Y'
selectAdminWriterSearchList=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT W.WRITER_NO, M.MEMBER_NICKNAME, W.WORK_TITLE, W.WORK_SUMMARY, W.WORK_NO, W.SECRET_FLAG FROM TB_WORK W JOIN TB_WRITER WR ON (W.WRITER_NO = WR.MEMBER_NO) JOIN TB_MEMBER M ON (WR.MEMBER_NO = M.MEMBER_NO) WHERE W.WRITER_NO = ? AND APPROVAL_STATUS = 'Y' AND W.WORK_TITLE LIKE ('%' || ? || '%') ORDER BY W.WORK_NO DESC) A) WHERE RNUM BETWEEN ? AND ?

#admin-episode
getAdminEpisodeCnt=SELECT COUNT(*) FROM TB_EPISODE JOIN TB_WORK USING(WORK_NO) WHERE WORK_NO = ? APPROVAL_STATUS = 'Y'
selectAdminEpisode=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT W.WORK_NO, W.WORK_TITLE, M.MEMBER_NICKNAME, EPISODE_NO, E.SECRET_FLAG, E.EPISODE_TITLE FROM TB_WORK W JOIN TB_WRITER WR ON (W.WRITER_NO = WR.MEMBER_NO) JOIN TB_MEMBER M ON (WR.MEMBER_NO = M.MEMBER_NO) JOIN TB_EPISODE E ON (W.WORK_NO = E.WORK_NO) WHERE W.WORK_NO = ? AND APPROVAL_STATUS = 'Y' ORDER BY W.WORK_NO DESC) A) WHERE RNUM BETWEEN ? AND ?
getAdminEpiSearchCnt=SELECT COUNT(*) FROM TB_EPISODE WHERE WORK_NO = ? AND EPISODE_TITLE LIKE ('%' || ? || '%') AND APPROVAL_STATUS = 'Y'
selectAdminEpiSearch=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT W.WORK_NO, W.WORK_TITLE, M.MEMBER_NICKNAME, EPISODE_NO, E.SECRET_FLAG, E.EPISODE_TITLE FROM TB_WORK W JOIN TB_WRITER WR ON (W.WRITER_NO = WR.MEMBER_NO) JOIN TB_MEMBER M ON (WR.MEMBER_NO = M.MEMBER_NO) JOIN TB_EPISODE E ON (W.WORK_NO = E.WORK_NO) WHERE W.WORK_NO = ? AND EPISODE_TITLE LIKE ('%' || ? || '%') AND APPROVAL_STATUS = 'Y' ORDER BY W.WORK_NO DESC) A) WHERE RNUM BETWEEN ? AND ?

#admin-work, episode approv
getWorkApprovListCount=SELECT COUNT(*) FROM TB_WORK WHERE APPROVAL_STATUS = 'N'
adminWorkApprovList=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT W.WORK_NO, W.WORK_TITLE, M.MEMBER_NICKNAME, W.REQUEST_DATE FROM TB_WORK W JOIN TB_WRITER WR ON (W.WRITER_NO = WR.MEMBER_NO) JOIN TB_MEMBER M ON (WR.MEMBER_NO = M.MEMBER_NO) WHERE APPROVAL_STATUS = 'N' ORDER BY W.WORK_NO DESC) A) WHERE RNUM BETWEEN ? AND ?
getWorkApprovSearchCnt=SELECT COUNT(*) FROM TB_WORK WHERE APPROVAL_STATUS = 'N' AND WORK_TITLE LIKE ('%' || ? || '%')
selectWorkApprovSearch=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT W.WORK_NO, W.WORK_TITLE, M.MEMBER_NICKNAME, W.REQUEST_DATE FROM TB_WORK W JOIN TB_WRITER WR ON (W.WRITER_NO = WR.MEMBER_NO) JOIN TB_MEMBER M ON (WR.MEMBER_NO = M.MEMBER_NO) WHERE APPROVAL_STATUS = 'N' AND WORK_TITLE LIKE ('%' || ? || '%') ORDER BY W.WORK_NO DESC) A) WHERE RNUM BETWEEN ? AND ?
getApprovWork=SELECT WORK_TITLE, M.MEMBER_NICKNAME, WORK_SUMMARY, WORK_PLOT, W.WORK_NO FROM TB_WORK W JOIN TB_MEMBER M ON (W.WRITER_NO = M.MEMBER_NO) WHERE WORK_NO = ? AND APPROVAL_STATUS = 'N'
adminEpiApprovList=SELECT * FROM (SELECT ROWNUM RNUM, A.* FROM (SELECT M.MEMBER_NICKNAME, W.WORK_TITLE, E.EPISODE_TITLE, E.REQUEST_DATE FROM TB_WORK W JOIN TB_EPISODE E ON (W.WORK_NO = E.WORK_NO) JOIN TB_WRITER WR ON (W.WRITER_NO = WR.MEMBER_NO) JOIN TB_MEMBER M ON (WR.MEMBER_NO = M.MEMBER_NO) WHERE E.APPROVAL_STATUS = 'N' ORDER BY W.WORK_NO DESC) A) WHERE RNUM BETWEEN ? AND ?

#PJH
insertWork=INSERT INTO TB_WORK VALUES(SEQ_WNO.NEXTVAL, ?,?, SYSDATE , NULL,?, ?, 'N', DEFAULT, DEFAULT,?, NULL, DEFAULT, ?, ?)
insertWorkGenre = INSERT INTO TB_GENRE_WORK VALUES(? ,SEQ_WNO.CURRVAL)
selectWorkList=SELECT WORK_TITLE, THUMBNAIL_MODIFY, WORK_SUMMARY, WORK_NO FROM TB_WORK WHERE APPROVAL_STATUS='Y' AND WRITER_NO=?

